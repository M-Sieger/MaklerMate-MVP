# 📄 Environment Variables – MaklerMate-MVP

# ==============================================================================
# FRONTEND (React) – für lokale Entwicklung und Production
# ==============================================================================

# 🔐 Supabase Authentication & Database
# Registriere ein kostenloses Projekt auf https://supabase.com/dashboard
# Diese Keys sind "public-safe" (Row-Level-Security in Supabase aktiviert)

REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here

# Hinweis: Der ANON_KEY ist public-safe und kann im Frontend-Code verwendet werden.
# Supabase schützt Daten mit Row-Level-Security (RLS).


# ==============================================================================
# BACKEND (Server) – nur für lokale Entwicklung (server/gpt-proxy.js)
# ==============================================================================

# 🧠 OpenAI API Key
# Erstelle einen API-Key auf https://platform.openai.com/api-keys
# ⚠️ WICHTIG: Dieser Key DARF NICHT im Frontend verwendet werden!
# Er wird nur im lokalen Express-Server (server/gpt-proxy.js) benötigt.

OPENAI_API_KEY=sk-your-openai-api-key-here

# 🌐 Express Server Port (optional, default: 5001)
PORT=5001


# ==============================================================================
# VERCEL (Production) – über Vercel Dashboard konfigurieren
# ==============================================================================

# Die folgenden Variablen müssen im Vercel Dashboard unter:
# "Settings → Environment Variables" hinzugefügt werden:
#
# 1. OPENAI_API_KEY (wie oben)
# 2. SUPABASE_URL (wie REACT_APP_SUPABASE_URL, aber ohne REACT_APP_ Präfix)
# 3. SUPABASE_ANON_KEY (wie REACT_APP_SUPABASE_ANON_KEY, aber ohne REACT_APP_ Präfix)
#
# Vercel unterstützt beide Varianten:
# - REACT_APP_* für Frontend (CRA)
# - Ohne Präfix für Serverless Functions (api/generate-expose.js)


# ==============================================================================
# SETUP-ANLEITUNG
# ==============================================================================

# 1. Kopiere diese Datei:
#    cp .env.example .env

# 2. Fülle die Werte aus (siehe Kommentare oben)

# 3. Starte die Entwicklungs-Umgebung:
#    # Terminal 1: Express-Proxy für GPT-Calls
#    node server/gpt-proxy.js
#
#    # Terminal 2: React-Frontend
#    pnpm start

# 4. Öffne im Browser: http://localhost:3000


# ==============================================================================
# TROUBLESHOOTING
# ==============================================================================

# Problem: "Supabase env missing/invalid"
# Lösung: Prüfe, ob REACT_APP_SUPABASE_URL eine gültige URL ist (https://...)
#         und REACT_APP_SUPABASE_ANON_KEY gesetzt ist.

# Problem: "GPT-Proxy-Fehler"
# Lösung: Prüfe, ob OPENAI_API_KEY in .env gesetzt ist
#         und server/gpt-proxy.js läuft (node server/gpt-proxy.js).

# Problem: "401 Unauthorized" bei GPT-Calls (Production)
# Lösung: Melde dich an (Login-Page). Der Access-Token wird automatisch
#         aus der Supabase-Session gelesen und an /api/generate-expose gesendet.


# ==============================================================================
# SICHERHEITS-HINWEISE
# ==============================================================================

# ✅ .env ist in .gitignore (wird NICHT committed)
# ✅ REACT_APP_SUPABASE_ANON_KEY ist public-safe (Row-Level-Security)
# ❌ OPENAI_API_KEY NIE ins Frontend (nur Server-Side!)
# ❌ OPENAI_API_KEY NIE in Git committen (.env.example zeigt nur Platzhalter)

# Für Production (Vercel):
# - Env-Vars im Vercel Dashboard setzen (nicht in .env)
# - OPENAI_API_KEY als "Secret" markieren (nicht im Build-Log sichtbar)
